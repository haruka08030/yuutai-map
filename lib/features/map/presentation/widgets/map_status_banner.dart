import 'package:flutter/material.dart';

class MapStatusBanner extends StatelessWidget {
  const MapStatusBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
